<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Mi primera pagina!</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" href="/estiloslab1bulma.css">
  </head>

    <nav class="navbar has-shadow is-white" role="navigation" aria-label="main navigation">
        <div class="navbar-brand">  
          <img src="/images/logo.jpg" width="80" height="10">
          <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
          </a>
        </div>
      
        <div id="navbarBasicExample" class="navbar-menu">
          <div class="navbar-start">
            <a class="navbar-item" href="/">
              Home
            </a>  
            
            
              <a class="navbar-item" href="/error">
                Error 404
              </a>
              <% for (permiso of permisos) { %>
                <% if(permiso.permiso == 'adoptar_gatos') { %>
                <a class="navbar-item" href="/gatos">
                  Adopta un Gato
                </a>
                <% } %>
              <% } %>

              <% for (permiso of permisos) { %>
                <% if(permiso.permiso == 'ver_gatos') { %>
                <a class="navbar-item" href="/misgatos">
                Mis Gatos
                </a>
                <% } %>
              <% } %>
            </div>
          </div>
      
          <div class="navbar-end">
            <div class="navbar-item">
              <div class="buttons">
                <% if (username == '') { %>
                  <a href="/users/signup" class="button registro-naranja">
                      <strong>Registrarse</strong>
                  </a>
                  <a href="/users/login" class="button is-light">
                      Iniciar Sesión
                  </a>
              <% } else { %>
                  <span style="margin-right: 15px;">Hola <%= username %>! </span>
                  <a href="/users/logout" class="button is-danger">
                      Salir
                  </a>
              <% } %>
              </div>
            </div>
          </div>
        </div>
      </nav>

